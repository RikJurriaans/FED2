### 
# This module is for interaction between 
# the user and the computer.
###

module 'I'

# Change the camera to a first person camera.
# @param camera:Camera = the camera that should be transformed.
# @param charHeight:Int = the height of the character in meters.
# @return new orbitControls:OrbitControls
# firstPerson = (camera, charHeight) ->
#     newCam = camera
#     newCam.position.set(0, charHeight, 0)
#     controls = new THREE.OrbitControls(camera)

    


