var MoviesModel = function(url) {
    var url = url;
    
    function getAllMovies() {
    }

    return {
        getData: function(url) {
            return {
                movies: movies
            };
        }
    };
}

