var JsonModel = function(url) {
    var url = url;

    /* Loads data */
    function load(url) { }

    return {
        getData: function(url) {
            return {
                title: 'About page',
                description: 'Dit komt straks allemaal uit de model.'
            };
        }
    };
}

